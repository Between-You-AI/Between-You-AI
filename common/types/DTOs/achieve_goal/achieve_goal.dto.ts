interface FindExpertDTO {
  objective: string;
}

interface FindExpertDTO {
  expertName: string;
  details: string;
}

interface Task {
  id: number;
  name: string;
  objective: string;
  duration: string;
  budget: string;
}